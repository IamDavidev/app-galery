
/* . entonces((r) = > r. json())
    . entonces((resp) => {
      const { data } = resp;
      const dataGiffs = datos. mapa((i) => {
        const { url } = i. imágenes. downsized_medium;
        const { title, id } = i;
        consola.log(url ,title ,id )
        return { url, title, id };
      });
      consola.log(s)
      devolver datosGiffs;
    }); */
